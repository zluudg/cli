name: Release Artifacts
on:
  release:
    types: [published]

jobs:
  artifacts:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v2
      - name: Get rpmbuild
        run: sudo apt install rpmbuild -y
      - name: Build rpm
        run: make rpm
